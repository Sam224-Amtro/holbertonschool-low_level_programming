#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * mul -  that multiplies two integers
 * @a: first Parameter for number
 * @b: second parameter for number
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
