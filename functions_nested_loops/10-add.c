#include <stdio.h>
#include "main.h"

/**
 * add - the unction that adds two integers and returns the result
 * @a: The first number to be compiter
 * @b: The second number to be compiter
 *
 * Return: Always 0
 */
int add(int a, int b)
{
	return (a + b);
}
